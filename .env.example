# Supabase 配置（云同步需要）
# 不配也能用纯本地 IndexedDB
# 1. 注册 https://supabase.com → 建项目（Singapore 区域）
# 2. Settings → API 拿 Project URL 和 publishable key (anon)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
